/* Global resets and defaults */
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

/* Use Space Mono throughout */
body {
  font-family: 'Space Mono', monospace;
  font-weight: 400;
  line-height: 1.5;
  color: #60665f; /* or your chosen text color */
  background-color: #fff; /* or your chosen background color */
  padding: 1rem;
}

/* Keep the header simple, with sign-in on top right */
.site-header {
  display: flex;
  justify-content: flex-end;
  margin-bottom: 2rem;
}

.site-header nav ul {
  list-style: none;
}

.site-header nav ul li {
  display: inline-block;
  margin-left: 1rem;
}

.site-header nav a {
  text-decoration: none;
  color: #60665f;
  font-weight: 400;
}

.site-header nav a:hover {
  text-decoration: underline;
}

/* Hero section styles */
.hero {
  max-width: 600px;
  margin-bottom: 3rem;
}

/* Big bold heading for the hero */
.hero h1 {
  font-size: 3rem;
  margin-bottom: 1rem;
  font-weight: 400; /* or 700 if you want it bolder */
  line-height: 1.2;
}

/* Sub‐header or subtitle styling */
.hero .subtitle {
  font-size: 1.25rem;
  margin-bottom: 1rem;
  color: #60665f; /* slightly different shade or keep same */
}

/* Content blocks styling */
.content-block {
  max-width: 600px;
  margin-bottom: 2rem;
}

.content-block h2 {
  font-size: 1.5rem;
  margin-bottom: 0.5rem;
  font-weight: 400;
}

/* Link styling for the body text */
.content-block a {
  text-decoration: none;
  color: #007BFF; /* placeholder link color */
  font-weight: 400;
}

.content-block a:hover {
  text-decoration: underline;
}
